spring:
  application:
    name: rmm-service
  profiles:
    active: des
    url: jdbc:postgresql://${PG_HOST:localhost}:5432/${POSTGRES_DB:ninja}
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect



